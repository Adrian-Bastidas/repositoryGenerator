jobs:
  - job: Diagnostico
    pool:
      name: Default
    steps:
      - bash: |
          echo "newRepoName   : ${{ parameters.newRepoName }}"
          echo "projectName   : ${{ parameters.projectName }}"
          echo "repoTemplate  : ${{ parameters.repoTemplate }}"
          echo "repoVisibility: ${{ parameters.repoVisibility }}"
          echo "runId         : ${{ parameters.runId }}"
